= simple_form_for(@comment) do |f|

  = f.error_notification

  .form-inputs
    = f.input :author, label: 'Имя:'
    = f.input :text, label: 'Комментарий'

  .form-actions
    = f.submit 'Отправить'
